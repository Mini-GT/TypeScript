// type User = {
//   username: string,
//   role: "guest" | "member" | "admin",
// }
// //unions is just like what we know in boolean where it is only true || false and it cannot be in other names just like in unions and this is like how booleans work but in a custom way
// type UserRole = "guest" | "member" | "admin";

// let userRole: UserRole = "member";